# 실데이터 연동 계획 문서 반영 프롬프트

## 작성시각
2026-03-21 19:45:34 KST

## 프롬프트 내용
```text
PLEASE IMPLEMENT THIS PLAN:
# 문서 전용 실데이터 연동 계획 반영

## Summary
- 이번 작업은 `소스코드 구현 없이 문서만` 변경한다.
- 기존 `docs/plan.md`는 유지하되, 하단에 `실데이터 연동 계획` 섹션을 새로 추가한다.
- 새 실행 문서는 `docs/task.md`로 만든다.
- 두 문서 모두 `Supabase 중심`, `KR 전체 + US 전체`, `GitHub Actions 하루 1회`, `시장 데이터만 실데이터`, `개미 광장/생존 일지는 1차 실데이터 제외` 기준으로 고정한다.
```

## 이번 단계 해석
- `docs/plan.md`는 기존 내용을 유지하고 하단에 새 계획을 덧붙인다
- `docs/task.md`는 구현 체크리스트 문서로 새로 만든다
- 소스코드는 수정하지 않는다
- 추가 기록 문서는 AGENTS 규칙에 맞춰 남긴다

## 결과
- `docs/plan.md` 업데이트
- `docs/task.md` 생성
- `docs/개발일지/...` 기록 추가
- `docs/prompt/...` 기록 추가
