# 적재범위 개편 v2 구현 프롬프트

## 작성시각
2026-03-22 20:28:00 KST

## 프롬프트 내용
```text
PLEASE IMPLEMENT THIS PLAN:
# Soyeon 적재 범위 개편 계획 v2
...
```

## 이번 단계 해석
- direct-read 구조는 유지해야 했다.
- KR/US hot 200, warm rotation, exact worker 수동 큐, stale 상태, failure log, coverage tier를 실제 코드와 migration에 반영해야 했다.
- KR 공급자는 구조상 KRX 우선이지만 현재 키가 없으므로 즉시 `yfinance_fallback`으로 돌아가게 해야 했다.
- 구현 후 `pnpm lint`, `pnpm typecheck`, `pnpm test`, `python -m pytest backend/tests`까지 증거를 남겨야 했다.

## 결과
- 배치 스크립트가 phase dispatcher 구조로 바뀌었다.
- coverage/state/failure/request queue 스키마와 public view 메타데이터 노출이 추가됐다.
- KR provider가 `KRX key 존재 여부`에 따라 `krx_official` 또는 `yfinance_fallback`으로 전환되게 됐다.
- 품질게이트와 백엔드 테스트를 모두 통과했다.
