# 프론트 접근성 보안 강화 프롬프트

## 작성시각
2026-03-22 23:37:41 KST

## 프롬프트 내용
```text
[HIGH] 줌을 막는 viewport 설정이 그대로 남아 있어 접근성을 깨고 있어. index.html:5 의 user-scalable=no는 Vercel 웹 인터페이스 가이드라인에서도 바로 플래그되는 패턴이야. 저시력 사용자 확대를 막아서 실제 사용성 문제가 생길 수 있어.

[HIGH] 기분 선택 카드가 키보드 접근이 안 돼. index.html:647 부근의 .mood-card는 실질적으로 선택 UI인데, 실제 마크업은 클릭만 받는 label이고 index.html:819 에서도 마우스 클릭만 바인딩하고 있어. 라디오 입력과 연결되거나 button/키보드 핸들링이 있어야 접근성 기준에 맞아.

[MEDIUM] 생존일지 입력창에 연결된 레이블이 없어 스크린리더 문맥이 약해. index.html:667 의 textarea는 placeholder만 있고 <label for="diary-entry-input"> 또는 aria-label이 없어. 지금 보이는 제목 생존 일기는 시각적으로는 충분하지만 보조기기 기준으로는 명시 연결이 없어.

innerHTML 제거 또는 escape 처리
/api/health 응답 최소화
```

## 이번 단계 해석
- 접근성 리뷰에서 나온 viewport, 키보드 접근, textarea label 이슈를 실제 코드로 반영해야 했다.
- 보안 리뷰에서 나온 `innerHTML` 기반 XSS 가능성과 health 응답 정보 노출도 같이 줄여야 했다.
- 수정 후에는 프론트 테스트와 backend health 테스트까지 같이 확인해야 했다.

## 결과
- viewport 확대 제한을 제거했다.
- 기분 선택은 radio 기반, textarea는 label 연결 구조로 바꿨다.
- 검색 결과와 생존일지 기록 렌더는 `textContent` 기반 DOM 생성으로 바꿨다.
- `/api/health`는 최소 응답만 반환하게 줄였다.
- `pnpm lint`, `pnpm typecheck`, `pnpm test`, `python -m pytest backend/tests/test_health_api.py`를 통과했다.
